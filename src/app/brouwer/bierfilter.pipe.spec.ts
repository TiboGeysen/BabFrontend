import { BierfilterPipe } from './bierfilter.pipe';

describe('BierfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BierfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
