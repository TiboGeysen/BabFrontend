import { BrouwerfilterPipe } from './brouwerfilter.pipe';

describe('BrouwerfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new BrouwerfilterPipe();
    expect(pipe).toBeTruthy();
  });
});
